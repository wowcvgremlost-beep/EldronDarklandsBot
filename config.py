"""
📁 config.py - Конфигурация бота
Здесь хранится токен от BotFather
"""

import os
from dotenv import load_dotenv

# Загружаем переменные из .env файла
load_dotenv()

# Токен бота (берётся из переменных окружения Railway)
BOT_TOKEN = os.getenv("BOT_TOKEN")

# Проверка: если токен не найден — останавливаем бота
if not BOT_TOKEN:
    raise RuntimeError("❌ BOT_TOKEN не найден в переменных окружения!")

# 🔧 ID администратора (ваш Telegram ID)
# Найдите свой ID через бота @userinfobot в Telegram
ADMIN_IDS = [566546686]  # Добавьте свои ID через запятую: [123456, 789012]
